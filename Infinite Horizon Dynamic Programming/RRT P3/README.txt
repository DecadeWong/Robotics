There are two main files in this directory, in the top directory the main file is for maze. 
The second file is for triangle which is in the src, if you want to run tri file, replace the top main file.

normally, it takes time to run the maze to find the path.